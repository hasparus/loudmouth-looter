{
  "//": "The editor was spun out of dungeon-motion and typechecks at the `strict` level (not Astro's `strictest`) with DOM.Iterable. It was migrated from React to Solid (see git history), so it uses the Solid JSX runtime here; the rest of the site stays at `strictest` (see ../../tsconfig.json).",
  "extends": "astro/tsconfigs/strict",
  "compilerOptions": {
    "lib": ["DOM", "DOM.Iterable", "ESNext"],
    "jsx": "preserve",
    "jsxImportSource": "solid-js",
    "skipLibCheck": true,
    "types": ["bun"]
  },
  "include": ["."]
}
